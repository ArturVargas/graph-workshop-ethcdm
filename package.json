{
  "name": "hack-gho",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ hack-gho",
    "create-local": "graph create --node http://localhost:8020/ hack-gho",
    "remove-local": "graph remove --node http://localhost:8020/ hack-gho",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 hack-gho",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.67.2",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
